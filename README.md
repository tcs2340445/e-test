# e-test
this is for some test


git checkout main

git commit -a -m "dev again"

git log --graph 

git rebase -i main 

git reset HEAD^

git checkout -b xxxx

git push

git merge dev

git add .

git clone -b branchName https://xxx.xxx.git
